---
'@open-slide/core': minor
---

Add a Themes panel to the dev UI. Themes under `themes/<id>.md` are now listed in the sidebar with a live preview rendered from a paired `themes/<id>.demo.tsx` slide module. The `/create-theme` skill now writes both files so every new theme has an instant preview.
