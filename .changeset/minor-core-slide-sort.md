---
'@open-slide/core': minor
---

Add slide list sorting on the home page by creation time or name (asc/desc). Reads a new optional `meta.createdAt` (ISO 8601 string) on each slide; preference persists per browser; default is newest-created first.
